import React, { Component } from 'react';

const ArtistInfo = (props) => {
  if (!props.artistInfo) {
    return (<div></div>)
  }

  return (
    <div>

    </div>
  );
};

export default ArtistInfo;
